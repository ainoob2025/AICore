agents:
  research:
    role: "research"
    description: "Research agent for documents and web-like sources."
    enabled: true
    tools:
      - "browser"
      - "rag"
      - "file"

  code:
    role: "code"
    description: "Code agent for writing and editing code."
    enabled: true
    tools:
      - "file"
      - "terminal"

  debug:
    role: "debug"
    description: "Debug agent for troubleshooting errors."
    enabled: true
    tools:
      - "file"
      - "terminal"

  websearch:
    role: "websearch"
    description: "Websearch-style agent using browser and RAG."
    enabled: true
    tools:
      - "browser"
      - "rag"

  memory:
    role: "memory"
    description: "Agent for memory and knowledge management."
    enabled: true
    tools:
      - "rag"
      - "file"

  workflow:
    role: "workflow"
    description: "Agent for orchestrating multi-step workflows."
    enabled: true
    tools: []